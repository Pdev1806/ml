import torch
import torch.nn as nn
#basically using a build in function to create a linear reg model
#built in functions for loss and optimization(no need to do update calucations manually 
#ie w -= learning_rate * w.grad)
